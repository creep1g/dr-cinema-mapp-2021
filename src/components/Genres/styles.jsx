import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  subtext: {
    color: 'black',
    textAlign: 'center',
    fontSize: 12,
  },
  contianer: {
    marginTop: 15,
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
    flexDirection: 'row',
  },
});
